# Model weights

## File

| File | Description |
|------|-------------|
| `front_rim_arc_v2.pt` | YOLO11s front-rim detector (training export V2) |

This file is included in the repository. The pipeline loads it from `src/config.py` (`WEIGHTS_PATH`).

Legacy weight files (for example `front_rim_best.pt`) are not published.
